language: node_js
sudo: required
node_js:
  - "10.1.0"
install: npm install
script: 
  ng build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN  # Set in the settings page of your repository, as a secure variable
  keep-history: true
  on:
    branch: master